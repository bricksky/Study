#include <stdio.h>
int main(void)

{
    printf("학번: %d\n", 20222284); // 정수는 %d로 출력한다.
    printf("이름: %s\n", "김동현"); // 문자열는 %s로 출력한다.   문자열은 큰 따옴표 ""를 붙여야 한다!
    printf("학점: %c\n", 'A');      // 문자는 %c로 출력한다.   문자는 작은 따옴표 ''를 붙여야 한다!
    return 0;
}
